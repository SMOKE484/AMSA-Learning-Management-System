// src/components/layout.ts
// Shared layout constants — imported by screens and App.tsx to avoid circular deps

export const TAB_BAR_HEIGHT = 68;
export const TAB_BAR_BOTTOM_OFFSET = 20; // how far above the edge the tab bar floats
